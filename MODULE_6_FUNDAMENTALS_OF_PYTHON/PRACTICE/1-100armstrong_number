for number in range(1, 1001):
    num = number
    digits = len(str(number))
    sum = 0

    while num > 0:
        digit = num % 10
        sum += digit ** digits
        num //= 10

    if sum == number:
        print(number)